<Window x:Class="CybersecurityChatbot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cybersecurity Chatbot" Height="650" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with ASCII Art -->
        <Border Grid.Row="0" Background="#FF2D2D30" Padding="10">
            <StackPanel>
                <TextBlock x:Name="AsciiArt" TextWrapping="Wrap" 
                          FontFamily="Consolas" FontSize="10" 
                          Foreground="LightGreen" HorizontalAlignment="Center"/>
                <TextBlock Text="Welcome to Cybersecurity Chatbot" 
                          HorizontalAlignment="Center" FontSize="16" 
                          Foreground="White" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Chat Display -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <TextBox x:Name="ChatDisplay" IsReadOnly="True" 
                    TextWrapping="Wrap" FontSize="14" 
                    Background="#FF252526" Foreground="White"
                    BorderThickness="0" Padding="10"/>
        </ScrollViewer>

        <!-- Input Area -->
        <Grid Grid.Row="2" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="UserInput" FontSize="14" Padding="5"
                    KeyDown="UserInput_KeyDown" Background="#FF3F3F46"
                    Foreground="White" BorderBrush="#FF007ACC"/>
            <Button x:Name="SendButton" Grid.Column="1" Content="SEND" 
                    Width="80" Margin="5,0,0,0" Click="SendButton_Click"
                    Background="#FF007ACC" Foreground="White"/>
        </Grid>
    </Grid>
</Window>
